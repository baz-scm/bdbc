{
  "name": "dbclient",
  "version": "1.0.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/server.ts",
    "build": "bun build --compile --minify src/server.ts --outfile dbclient"
  }
}
